import React from 'react';
import Card from '../../components/Card';

const Bookings = () => {
  return (
    <div>
      <Card>
        <h3>Manage Bookings</h3>
        <p className="text-muted" style={{marginTop: '8px'}}>Calendar and list of bookings will appear here.</p>
      </Card>
    </div>
  );
};

export default Bookings;
